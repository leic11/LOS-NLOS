# constants.py
"""
STCA 模型常量配置

论文最优超参数 (Zeng et al. 2024, Table III)
"""

# ============================================================================
# 空间编码器 (AAM Module) 默认参数
# ============================================================================
SPATIAL_EMBED_DIM = 64          # 空间嵌入维度
SPATIAL_NUM_HEADS = 1           # 注意力头数
SPATIAL_NUM_LAYERS = 1          # 编码器层数
SPATIAL_D_FF = 128              # 前馈网络隐藏层维度 (embed_dim * 2)
SPATIAL_DROPOUT = 0.5           # Dropout 比率（从 0.2 提升至 0.5，类似 BaseModel 的 0.6）

# ============================================================================
# 时间编码器 (LSTM-TFE Module) 默认参数
# ============================================================================
TEMPORAL_EMBED_DIM = 64         # 时间嵌入维度
TEMPORAL_NUM_LAYERS = 1         # LSTM 层数
TEMPORAL_DROPOUT = 0.5          # Dropout 比率（从 0.2 提升至 0.5，防止过拟合）
TEMPORAL_BIDIRECTIONAL = False  # 单向 LSTM

# ============================================================================
# 交叉注意力模块默认参数
# ============================================================================
CROSS_ATTN_EMBED_DIM = 64       # 交叉注意力嵌入维度
CROSS_ATTN_NUM_HEADS = 1        # 注意力头数
CROSS_ATTN_DROPOUT = 0.5        # Dropout 比率（从 0.2 提升至 0.5，防止过拟合）

# ============================================================================
# 稀疏表示模块默认参数
# ============================================================================
SPARSE_EMBED_DIM = 64           # 稀疏嵌入维度（与交叉注意力输出一致）

# ============================================================================
# 分类器默认参数
# ============================================================================
CLASSIFIER_HIDDEN_DIMS = [64, 32]  # 隐藏层维度
CLASSIFIER_DROPOUT = 0.6           # Dropout 比率（保持 0.6，与 BaseModel 一致）

# ============================================================================
# 训练默认参数
# ============================================================================
BATCH_SIZE = 32               # 增加 batch size 以稳定梯度（与 BaseModel 一致）
EPOCHS = 50                   # 固定训练轮数
LEARNING_RATE = 0.001         # 学习率 0.001（配合 L2 正则化使用）
RANDOM_SEED = 42

# 正则化参数
WEIGHT_DECAY = 1e-4           # L2 正则化系数（1e-4 温和正则化）

